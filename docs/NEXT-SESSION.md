# برومت الجلسة القادمة

> **هذا الملف ليس حالة المشروع.** الحالة في [`STATUS.md`](STATUS.md) والخطة في [`PLAN.md`](PLAN.md)، **وحقيقتان للمشروع أسوأ من واحدة**.
> ما هنا هو ما لا يُستنتج من الملفات وحدها: **من أين تبدأ الجلسة، وعلى أي فرع، وما القرار المعلَّق على المالك**.
> آخر تحديث: 2026-08-22.

## كيف يُستعمل

انسخ كل ما بين السطرين أدناه، والصقه أول رسالة في الجلسة الجديدة. **ولا تلخّصه**: كل بند فيه موجود لأن غيابه كلّف وقتاً في جلسة سابقة.

---

```text
مشروع TransferChecker: تصحيح أوراق الإجابة بالجوال (OMR)، عربي أولاً.

اقرأ أولاً، ولا تبدأ قبل أن تقرأ:
1. docs/STATUS.md كاملاً. هو وثيقة التسليم.
2. docs/PLAN.md الأقسام 5 و6 و7.
3. إدخالات 2026-08-22 و2026-08-21 في docs/DEVLOG.md. الأحدث أولاً، ثم
   نزولاً إلى 2026-08-20 لأن جولة الورق الحقيقي والإصلاحات الستة هي
   سياق كل ما بعدها.
4. docs/SERVER.md إن كان العمل يمس الخادم أو النشر.
5. .claude/skills/tc-interface إن كان العمل يمس أي واجهة، قبل أي مهارة أخرى.

الفرع: claude/transferchecker-omr-mobile-9ogs9v
الدفع عليه وحده. ولا تفتح طلب دمج إلا إن طلبتُه صراحةً.

أين وصلنا:
- سبع حزم وثلاثة تطبيقات، و710 اختبارات تمر.
- طبقة قاعدة البيانات مبنية في packages/db: ثلاثة عشر جدولاً،
  وعزل صفوف على كل جدول، مفحوص بـPostgreSQL حقيقية تُقلع داخل الاختبار.
- الاستضافة SCCC لا Supabase. نسخة PostgreSQL 18 تعمل في الرياض،
  والمهاجرات مطبَّقة عليها، وعشرة فحوص عزل خضراء عليها هي لا على محاكاة.
- دور الاتصال tc_app منشأ ومفحوص بلا BYPASSRLS، وtc_owner للمهاجرات وحدها.
- المصادقة مكتملة منطقاً: التسجيل، وكلمة السر بـArgon2id، والدخول بتأخير
  متصاعد بلا قفل، ودوران رمز التحديث، وإصدار رمز الوصول والتحقق منه
  (JWT بتوقيع Ed25519 بشكل GoTrue حرفياً، عبر jose)، وتأكيد البريد برمز
  مخزَّن. الناقص اختيار مزوِّد البريد وحده، وهو قرار تجاري عند المالك.
- خادم التطبيق مبني في apps/server: عشر نقاط على node:http بلا تبعية،
  المصادقة كاملة ومعها إعادة إرسال التأكيد، وواجهة المزامنة (دفع
  بالدفعات وسحب تفاضلي)، بقبعتي tc_auth وauthenticated، ومفحوص على
  السلك. ورسالة التأكيد مكتوبة بثماني لغات فالمزوّد ينقل ولا يؤلّف.
  نشره موثق في docs/SERVER.md. ولا يوجد تطبيق جوال.

وسياق المحرك، من 2026-08-20 و21:
- وصل أول ورق حقيقي: أربع أوراق و19 لقطة، مصوَّرة بالجوال.
- وكشفت أن المحرك يقرأ التظليل المصمت بـ100% ويقرأ خطّ القلم كما يقرأ
  الورق الأبيض، لأن الناس يرسمون لفّات وخطوطاً مائلة لا تظليلاً.
- فنزلت ستة إصلاحات مقيسة: قياس الفقاعة بقرصين، وشبكة الهوية، وخانة
  نسخة المفتاح، ولون المستورِد، ومحمّل المجموعة، وعيب قياس في المسطرة.
- والصمت على اللفّات نزل من 9 حالات من 25 إلى 5، والورقة الفارغة لم
  تتحرك: quick20 وstandard50 وfull100 كلها تُقرأ فارغة تماماً.
- والأوراق الأربع محفوظة kind: 'fixture' فلا تسلّح بوابة الدقة.

وما صار في 2026-08-21 و22، وهو أحدث شيء، خمس جولات مدفوعة كل واحدة وحدها:
1. ربط نسخة المفتاح من طرف إلى طرف (21): عمود form في scans بثلاث حالات
   (بلا خانة، وخانة فارغة، ونسخة معلنة)، وexamDataOf يعيد نسخة كل ورقة،
   وgradeStored يحملها إلى الدرجة، فالخانة الفارغة تفرض المراجعة على
   الشاشة أيضاً لا على الجهاز وحده. مهاجرة 0004.
2. ضم مهارات الواجهة بعد فحص أمني (21): سبع مهارات MIT من
   emilkowalski/skills وui-ux-pro-max، ومهارة حاكمة مؤلَّفة عندنا اسمها
   tc-interface تربطها بنظام التصميم. والمستبعدات وأسبابها في
   .claude/skills/README.md، ومنها مستودع بلا رخصة رُفض نسخه.
3. المصادقة اكتملت (22): رمز وصول JWT بتوقيع Ed25519 عبر jose بحمولة
   GoTrue حرفياً (فانتقال لاحق لخدمة مدارة لا يعيد كتابة سياسة)، وتأكيد
   بريد برمز مخزَّن مجزَّأ لا رابط موقَّع. مهاجرة 0005.
4. دور خدمة المصادقة tc_auth (22): باب مسمّى بسياسات لا استثناء من
   العزل، وسياجه في الاتجاهين: لا يقرأ درجة واحدة، ولا عميل يقرأ بصمة.
   مهاجرة 0006.
5. الخادم والمزامنة (22): apps/server بتسع نقاط على node:http بلا
   تبعية، ومزامنة بدفعات المئة وسحب تفاضلي بمؤشر متأخر نافذة أمان
   عشر ثوان. مهاجرة 0007.
6. رسالة التأكيد ومصائد المستورِد (22)، خمس دفعات مستقلة: مهلة خطّاف
   كانت تُتخطّى بها 21 اختباراً بصمت؛ ورسالة التأكيد مكتوبة بثماني
   لغات (واللغة كانت تُجمع وتُرمى، وإخفاق الإرسال كان يُسقط تسجيلاً
   مكتملاً)؛ ونقطة إعادة إرسال برمز وصول؛ وأربع مصائد في import-shot
   صارت رفضاً ومعها أول اختباراتها؛ وإحصاءة زمنية كانت تنحاز للنصف
   الأقصر فتُحمّر البناء بلا سبب في المحرك. بلا مهاجرات.

التالي، والنشر ليس منه، وهذا مقصود:
1. تطبيق المهاجرات الأربع 0004 إلى 0007 على نسخة الرياض، ثم verify،
   من أي جهاز على قائمة السماح. مجاني ودقائق، وقيمته أن المخطط في
   المستودع والمخطط في الرياض يبقيان متطابقين. غير عاجل لأن لا شيء
   يكتب في تلك القاعدة بعد، لكنه أول ما يُنجز أمام كونسول SCCC.
2. وتشغيلة دخان واحدة للخادم من الجهاز نفسه ثم إطفاؤه. القاعدة التي
   تقلع داخل الاختبار تثبت المهاجرات والسياسات ولا تثبت سلسلة الأدوار
   على النسخة الحقيقية: هل tc_app يلبس tc_auth بعد منح 0006، وهل SSL
   والتجميع يعملان. ربع ساعة تجيب هذا، بلا كلفة مستمرة.
3. الخطوة البرمجية الكبيرة التالية هي تطبيق الجوال، وهي خلف بوابة
   الثلاثين ورقة (قاعدة القسم 13). وما قبلها أعمال واجهة اختيارية
   بمهارات tc-interface (منشئ النماذج، وتطوير صفحة الهبوط) إن طُلبت.

والنشر الدائم مؤجَّل عمداً: لا شيء ينادي الخادم اليوم (لا جوال، ولوحة
التحكم مولّد ثابت لا يقرأ منه، ولا مستخدمين)، وجهاز ECS يعمل على مدار
الساعة كلفة شهرية بلا مقابل، ورسالة التأكيد لن تُرسل أصلاً قبل اختيار
مزود البريد. يُنشر حين يوجد من ينادي: الجوال جاهزاً للمزامنة، أو بيتا
بمستخدمين حقيقيين. والخطوات جاهزة في docs/SERVER.md لذلك اليوم.
وسؤال RDS Supabase حُسم بحثاً في 2026-08-21: الخدمة محصورة في مناطق
صينية ولا تتوفر في me-central-1، فبُنيت المصادقة بأيدينا، وشكل الحمولة
GoTrue حرفياً يبقي باب الانتقال مفتوحاً بلا إعادة كتابة سياسة.

وشاشة الكاميرا ليست التالي، بقاعدة القسم 13 من الخطة: تنتظر ثلاثين ورقة.

وعملي أنا لا عملك، يجري بالتوازي: جمع الورق. عندي أربع أوراق، ينقص
الحمراء لقطة ثالثة، والهدف ثلاثون ورقة تُسلّح بوابة الدقة أول مرة.

قرار معلَّق عليّ أنا لا عليك: خدمة إرسال البريد وسعرها. اعرضها عليّ
بالخيارات والأسعار حين تصل إليها، ولا توقف بقية العمل انتظاراً لها.

قواعد إلزامية:
- وثّق أي تعديل في docs/DEVLOG.md قبل دمجه.
- التعليقات في الكود بالإنجليزية، والوثائق بالعربية.
- لا فاصلة مطولة في أي نص يراه مستخدم، ويفرضها scripts/check-no-emdash.mjs
- الفحوص الخمسة خضراء قبل أي دفع: typecheck، test، eslint، فحص الفاصلة،
  format:check
- ادفع بعد كل إصلاح، لا تجمعها. الآلة تُعاد تشغيلها بلا إنذار وكل ما
  ليس مدفوعاً يضيع. حصل ثلاث مرات في 2026-08-21 ولم يضع شيء بسبب هذا.
- تحقق من حداثة أي أداة بالتشغيل أو بالبحث لا بالذاكرة.
- لا تصدّق نتيجة وكيل بلا تحقق بالتشغيل.
- افحص كل اختبار جديد بالطفرة: أزل الإصلاح وتأكد أن الاختبار يسقط.
  اختبار يمرّ قبل الإصلاح وبعده لا يبرهن شيئاً.
- sharp لا يدخل هذا المستودع أبداً، لا تبعيةً لـcore-omr ولا لـCI.
- العربية والإنجليزية في apps/site/src/content.ts مكتوبتان بيدي
  (source: 'owner')، ولا يعيد أي أداة توليدهما.
- بعد أي تغيير في المخطط: migrate ثم verify فوراً على النسخة الحقيقية.

وأنا ليست لدي خبرة برمجية. القرارات التقنية اتخذها أنت بأسس علمية وبالخطة
كما هي، ولا تعرض عليّ إلا القرارات التجارية.
```

---

## ولمن يعمل على الواجهة

**ابدأ بمهارة `tc-interface` في `.claude/skills/`.** هي الحاكمة: نظام التصميم
قانون لا تعلو عليه أي مهارة، والموقع بلا إطارات ولا تبعيات، والحركة بميزانية
القسم 4، وRTL أولاً. وبجانبها مهارات مضمومة برخصة MIT وفحص أمني موثق في
DEVLOG بتاريخ 2026-08-21: سبع من مستودع مؤلّفها emilkowalski/skills للحركة
والصقل، وui-ux-pro-max للتدقيق. والمستبعدات وأسبابها في `.claude/skills/README.md`.

## ما يحتاجه المطوِّر ولا يوجد في المستودع

- **بيانات اتصال القاعدة.** أربعة مطلوبة: `PGHOST` و`PGDATABASE` و`PGUSER` و`PGPASSWORD`، وثلاثة اختيارية: `PGPORT` (افتراضها 5432) و`PGSSL` و`PGPOOLED`. **ولا واحد منها في المستودع ولا يدخله**، ومصدرها الوحيد `packages/db/src/connect.ts`. **وأربع مهاجرات `0004` إلى `0007` تنتظرها**: أول آلة تحملها تشغّل `pnpm --filter @transferchecker/db migrate` ثم `verify`، قبل أي شيء آخر.
- **عنوان الجهاز في قائمة السماح.** القاعدة لا تجيب جهازاً غير مسموح له، **وتتصرف كأنها مطفأة**: `ETIMEDOUT` لا رفضاً. والاتصال المنزلي يأخذ عنواناً جديداً من نفسه، **فالعطل يعود من نفسه أيضاً**. `packages/db` يشرح هذا بجملة بدل أن يطبع أثر نداء.
- **`tc_owner` للمهاجرات وحدها.** بياناته لا تدخل إعداد التطبيق أبداً، **و`assertUnprivileged` يرفض الإقلاع إن دخلت**.

## ولمن يستورد لقطة: أربع مصائد أُغلقت في الأداة، واثنتان تبقيان حقيقتين

قيست الست في 2026-08-21 بالتشغيل، **وأربع منها صارت في 2026-08-22 رفضاً من الأداة نفسها بدل سطر في وثيقة**، لأن الأداة التي تبني حقيقة المجموعة كانت بلا اختبار واحد. التفصيل في `DEVLOG`.

| المصيدة                                     | حالها اليوم                                                                                             |
| ------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| `--root` منسيّ فيكتب داخل تجهيزة git        | **تُرفض**: الجذر يُقال صراحةً بالراية أو بـ`TRANSFERCHECKER_GOLDEN_ROOT`، ولا يُخمَّن أبداً             |
| `--template` منسيّ فيفترض `standard50` بصمت | **تُرفض** عند أول استيراد للورقة، وهو الموضع الوحيد الذي يقرر هندستها إلى الأبد                         |
| `--template` مخالف في لقطة لاحقة            | **تُرفض** بدل أن تُهمَل بصمت، ويُقال عدد الأسئلة عند الطرفين                                            |
| `--pen` فوق 40 حرفاً (وأخواته)              | **تُرفض قبل كتابة أي بايت**، بأسماء الرايات لا بمسارات السكيما، فلا يبقى مانيفست لا يُقرأ               |
| صور iPhone بصيغة HEIC                       | تفشل ما لم يحمل المفكِّك libheif، **والرسالة صارت تقول: صدّر JPEG من الجوال**                           |
| التثبيت العام لـ`sharp`                     | **لا يعمل** (ESM يحلّ من مسار الوحدة صعوداً)، والأداة تطبع الأمر الصحيح: تثبيت في مجلد أعلى من المستودع |
| الورقة الحقيقية: ثلاث لقطات وقارئان         | **ليست مصيدة بل قاعدة**. والناقصة تُسمّى وتُتخطّى ولا تُسقط أخواتها (إصلاح 2026-08-21)                  |

## وانحراف في البيئة يُسجَّل

**الآلة تعمل بـNode 22 والمشروع يشترط `>=24`.** لم يكسر شيئاً حتى 2026-08-21، **لكن اختلاف نسخة المشغّل هو بالضبط ما يجعل نتيجة تختلف بين آلتين**، فيُفحص أول ما يظهر عطل لا يُفسَّر.

## سؤال RDS Supabase: حُسم

فُحص بالبحث في 2026-08-21: الخدمة محصورة في مناطق صينية ولا أثر لها في منتجات SCCC المحلية، **فبُنيت المصادقة بأيدينا**. وشكل حمولة رمز الوصول GoTrue حرفياً (اختبار ذهبي يسمّره) **يبقي باب الانتقال مفتوحاً بلا إعادة كتابة سياسة ولا اختبار** إن توفرت الخدمة يوماً في me-central-1.
