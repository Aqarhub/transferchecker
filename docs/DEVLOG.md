# سجل التطوير (DEVLOG)

> **قاعدة إلزامية:** أي تعديل أو تطوير أو قرار تقني في هذا المشروع يجب توثيقه هنا قبل دمجه.
> كل إدخال يتبع النموذج أدناه، والأحدث يُكتب في الأعلى.

## نموذج الإدخال

```markdown
## [YYYY-MM-DD] عنوان مختصر للتغيير

- **النوع:** إضافة / تعديل / إصلاح / قرار تقني / أمان / توثيق
- **الوصف:** ماذا تغيّر ولماذا.
- **الملفات المتأثرة:** قائمة المسارات.
- **القرارات:** أي قرار تقني اتُّخذ ولماذا، والبدائل المرفوضة.
- **المراجع:** روابط أو مصادر إن وجدت.
```

---

## [2026-08-14] هيكل المستودع وسلسلة الفحص الآلي

- **النوع:** إضافة + قرار تقني.
- **الوصف:** تشغيل الـmonorepo فعلياً: pnpm 11.21 وTurborepo 2.10 وESLint 10 وVitest 4 وCI. كل الأوامر تعمل ومتحقق منها بالتشغيل لا بالافتراض: `typecheck` و`lint` و`test` و`format:check` و`check:emdash` تمر كلها نظيفة.
- **الملفات المتأثرة:** `package.json`، `pnpm-workspace.yaml`، `turbo.json`، `eslint.config.mjs`، `vitest.config.ts`، `tsconfig.base.json`، `tsconfig.json`، `.prettierrc`، `.prettierignore`، `.github/workflows/ci.yml`، `scripts/check-no-emdash.mjs`.
- **القرارات:**
  - إعدادات الأمان في pnpm 11 تبقى مفعّلة صراحةً في `pnpm-workspace.yaml`: تأخير 24 ساعة قبل اعتماد أي إصدار جديد، وحجب مصادر git/tarball للتبعيات غير المباشرة، و`allowBuilds` كقائمة سماح لسكربتات التثبيت.
  - `catalog:` لكل تبعية مشتركة، فلا يمكن أن تنجرف إصدارات الموبايل عن الويب.
  - **فاحص الشرطة الطويلة** يمسح TS وTSX وJSON وMDX، ويستثني توثيق المطورين (`.md`) لأن القاعدة تخص نصوص المستخدم لا وثائقنا. مُختبَر في الاتجاهين.
  - `fetch-depth: 0` في CI وليس 2: قاعدة `--affected` تحتاج merge base، وإن لم يوجد فإن Turborepo **يشغّل كل شيء بصمت** فيبقى CI أخضر بينما الفلترة ميتة.
  - كل GitHub Actions مثبتة بـcommit SHA كامل لا بالوسم.
  - لا عتبات تغطية في البداية، لأنها تفشل CI لسبب سياسي لا لخلل حقيقي. البوابة الحقيقية ستكون دقة المجموعة الذهبية.
- **مشاكل واقعية حلّها التشغيل الفعلي:**
  1. **مُبلّغ `basic` في Vitest محذوف** في الإصدار 4.
  2. **Vitest يلتقط إعداد الجذر** عند التشغيل من داخل الحزمة، فتُحل أنماط `projects` نسبةً للمجلد الخطأ وتطابق صفر مشاريع. الحل: إعداد vitest مستقل لكل حزمة.
  3. **`projectService` يرفض أي ملف لا يغطيه tsconfig**، بما فيها `vitest.config.ts` داخل الحزمة. الحل: كل ملف يدخل في `include`، وإسقاط `allowDefaultProject` لأنه يتعارض مع تغطية tsconfig الجذر.
  4. **`strictTypeChecked` يمنع إقحام الأرقام في القوالب النصية**، فكل تحويل صار صريحاً بـ`String()`.
  5. TypeScript 6 غيّر افتراضات صامتة: `strict` صار true، و`esModuleInterop` صار true، و**`target` صار عائماً**. لذلك `target` و`lib` مثبتان صراحةً وإلا غيّرت ترقية المحوّل ما نشحنه لـHermes.

## [2026-08-14] متطلب الاكتشاف: SEO و GEO و AEO

- **النوع:** إضافة + قرار تقني.
- **الوصف:** طلب المستخدم تحسين SEO وGEO وAEO بكل اللغات الأساسية، مع منع حجب زواحف محركات البحث وأنظمة الذكاء الاصطناعي. بحث موسع في مصادر 2026 الرسمية وأوراق محكّمة، والنتائج في `docs/DISCOVERABILITY.md`.
- **الملفات المتأثرة:** `docs/DISCOVERABILITY.md` (جديد)، `docs/PLAN.md` (قسم 8ب ومعياران جديدان للقبول وبند في الجدول الزمني).
- **القرارات:**
  1. **سياسة الفتح الكامل** لكل الزواحف، بمجموعة `robots.txt` واحدة. المجموعات المسمّاة مرفوضة لأن مجموعة `User-agent` المحددة **تحل محل** مجموعة `*` ولا ترث منها حسب RFC 9309، فنسيان أسطر `Disallow` فيها يفتح لوحة التحكم.
  2. **التصيير على السيرفر إلزامي** لكل صفحة تسويقية. قياس Vercel وMERJ على مئات الملايين من الطلبات: زواحف الذكاء الاصطناعي **لا تنفّذ JavaScript** (الاستثناءان Googlebot وApplebot). محتوى client-rendered غير موجود بالنسبة لها.
  3. **تعطيل حجب Cloudflare** بند إطلاق إلزامي مع أوامر تحقق، وخصوصاً إطفاء Managed robots.txt الذي يكتب فوق ملفنا بصمت.
  4. **نشر أرقام الدقة الحقيقية** من المجموعة الذهبية كاستراتيجية GEO أساسية، فإضافة الإحصاءات الموثّقة هي التكتيك الوحيد بدليل محكّم مكرر (نحو 30 إلى 40% في ورقة KDD 2024).
  5. **schema ببيانات حقيقية فقط.** أقوى دراسة متاحة وجدت أثر الـschema العامة صفراً إحصائياً، والاستثناء الوحيد هو الـschema بحقول سمات مملوءة، وأثره أوضح ما يكون على النطاقات الجديدة ضعيفة السلطة، وهو وصفنا.
  6. **llms.txt يُنشر كخيار رخيص لا كرافعة.** Google صرّحت أنها لا تستخدمه، و97% من هذه الملفات لا تتلقى أي طلب.
  7. **توقعات معايرة:** المسح النقدي 2026 يخلص إلى أن تكتيكات GEO تساعد على الفوز بالإجابة بعد دخول مجموعة الاسترجاع ولا تُدخل إليها. توزيع الجهد نحو 70% SEO تقليدي وسلطة، و30% تنسيق GEO.
- **المراجع:** ورقة GEO (KDD 2024، arXiv:2311.09735)، المسح النقدي (arXiv:2607.14035)، دراسة Vercel وMERJ عن زواحف الذكاء الاصطناعي، دليل Google لتحسين الظهور (مايو 2026)، توثيق Cloudflare AI Crawl Control.

## [2026-08-14] المرحلة الأولى: حزمة sheet-spec ومحرك التخطيط

- **النوع:** إضافة + إصلاح.
- **الوصف:** أول حزمة حقيقية في المشروع. `@transferchecker/sheet-spec` تحوّل مواصفة الورقة إلى إحداثيات بالمليمتر، وهي مصدر الحقيقة الوحيد لمولّد PDF وللماسح معاً. 29 اختباراً تمر، وفحص الأنواع نظيف بـTypeScript 6.0.3 مع كل الأعلام الصارمة.
- **الملفات المتأثرة:** `packages/sheet-spec/**`، `tsconfig.base.json`، `.gitignore`، `docs/mockups/*.svg`.
- **القرارات:**
  - `layoutSheet` دالة خالصة تعيد `LayoutResult` كـdiscriminated union. تركيبة لا تتسع في الورقة ليست استثناءً بل بيانات (`overflow` مع المنطقة والمحور والمقاسين)، والواجهة هي من يحولها إلى رسالة بلغة المستخدم.
  - `bubbleGroups` تسطّح التخطيط إلى مجموعات، وكل فقاعة تحمل رمزها. الماسح لا يحتاج أن يعرف نوع الحقل، فلا حالات خاصة في كود القراءة.
  - حقول الترويسة من نوع `writtenBox` تُرتّب في شريط الترويسة، ومن نوع `bubbleGrid` تُكدّس في شريط جانبي يمين. قرار يبسّط الخوارزمية ويطابق أوراق الإجابة الحقيقية.
  - العارض SVG أداة تطوير في `tools/` وليست واجهة عامة، لأن الإنتاج بـTypst. لكنه يستهلك نفس التخطيط، فالنماذج في `docs/mockups` صارت مولَّدة من الكود ولا يمكن أن تنحرف عنه.
- **إصلاحات كشفتها الاختبارات (قيمة الاختبار المبكر):**
  1. **ترتيب فحص المساحة كان يلوم الطرف الخطأ.** كان الشريط الجانبي يُفحص أولاً بمعيار "يكفي عمود أسئلة واحد"، فتظهر أوراق يمر فيها الشريط ثم تفشل الأسئلة. صار الترتيب: تُقاس شبكة الأسئلة أولاً على كامل عرض المحتوى (فهي المحتوى الأساسي)، ثم يُطلب من الشريط الجانبي أن يتسع فيما تبقى.
  2. **الأبعاد الافتراضية للفقاعات كانت سمينة.** بقطر 5.2mm لم تكن ورقة بثلاثة أعمدة وخمسة خيارات مع شبكة رقم طالب تتسع في A4، مع أن الأوراق التجارية تفعلها. القطر الآن 4.4mm (نحو 21 بكسل بعد التصحيح المنظوري إلى عرض 1000 بكسل، وهو أكثر من كافٍ لقياس نسبة التعبئة).
  3. **مولّد النماذج كان يكتب خارج المستودع.** حساب المسار من `import.meta.url` يشير لموقع الحزمة المبنية لا لموقع المصدر. المسار الآن من سطر الأوامر.
- **ملاحظة توافق:** مُبلّغ `basic` في Vitest حُذف في الإصدار 4 كما توقّع التحقق المسبق.

## [2026-08-13] مواصفة تصميم ورقة الإجابة v2 بمتطلبات المستخدم

- **النوع:** إضافة + قرار تقني.
- **الوصف:** استلام متطلبات تصميم الورقة من المستخدم مع صورة مرجعية بنمط ZipGrade. كتابة مواصفة التصميم التفصيلية وإنتاج نموذجين بصريين SVG (إنجليزي وعربي) للمصادقة على الشكل قبل بناء المولّد الحقيقي بـTypst.
- **الملفات المتأثرة:** `docs/SHEET-SPEC.md` (جديد)، `docs/mockups/sheet-mockup-en.svg` و`docs/mockups/sheet-mockup-ar.svg` (جديدة)، `docs/PLAN.md`.
- **القرارات:**
  - ترقية سكيما الورقة إلى v2: حقول ترويسة معرفة من المستخدم (أي تسمية بأي لغة، كتابة يدوية أو تظليل)، شبكة هوية طالب اختيارية وقابلة للاستبدال، نص branding وعنوان اختبار حران.
  - حروف الفقاعات قابلة للتبديل بين اللاتينية والعربية، مع بقاء هيكل الورقة والأرقام والاتجاه بالإنجليزية LTR كأصل ثابت.
  - الماسح يقرأ أي bubbleGrid بشكل عام (كل عمود = خانة) بلا كود خاص لكل حقل.
  - التركيبات التي لا تتسع في صفحة واحدة تُرفض برسالة واضحة عبر دالة `fits(spec)`، وتعدد الصفحات مؤجل خارج الـMVP.
- **المراجع:** صورة مرجعية من المستخدم بنمط ZipGrade، `docs/SHEET-SPEC.md`.

## [2026-08-13] تحديث الخطة بنتائج التحقق من الستاك (7 محاور بحث)

- **النوع:** قرار تقني + أمان + توثيق.
- **الوصف:** تحقق موسع من كل أدوات الستاك ضد مصادر أغسطس 2026 الرسمية (npm registry، مستودعات GitHub الرسمية، OWASP، NIST). كشف التحقق أن عدة اختيارات في الخطة الأصلية بُنيت على معلومات قديمة وتم تصحيحها.
- **الملفات المتأثرة:** `docs/TECH-STACK.md` (جديد)، `docs/PLAN.md`، `docs/CODING-STANDARDS.md`.
- **القرارات (مع البدائل المرفوضة):**
  1. **PDF عبر Typst 0.15 بدل pdf-lib.** السبب: pdf-lib مهجور منذ 2021 ولا يشكّل الحروف العربية إطلاقاً، ولا توجد مكتبة JS تفعل ذلك بشكل صحيح في 2026. البدائل المرفوضة: satori وpdfmake (بلا RTL)، @react-pdf/renderer (دعم عربي مكسور)، Playwright/Chromium (يبقى احتياطاً).
  2. **VisionCamera v5 (Nitro Modules) بدل v4 المؤرشف.** الرسم عبر `<SkiaCamera />` من حزمة `react-native-vision-camera-skia` لأن `useSkiaFrameProcessor` حُذف. worklets من Software Mansion بدل worklets-core.
  3. **expo-sqlite مع useSQLCipher بدل op-sqlite.** أصبح دعم SQLCipher رسمياً أولى الطرف ويتحدث مع الـSDK. op-sqlite يبقى بديل أداء موثقاً.
  4. **TypeScript 6.0.3 مثبّت بالضبط.** TS 7 (المحوّل بلغة Go) صدر لكن typescript-eslint يشترط أقل من 6.1، والفحص المعتمد على الأنواع أهم من سرعة المحوّل. يُراجع عند صدور TS 7.1.
  5. **Next.js 16.3 بدل 15.** وTurbopack أصبح الافتراضي و`proxy.ts` بدل `middleware.ts`.
  6. **كلمة السر 15 حرفاً حداً أدنى بدل 10.** NIST SP 800-63B المراجعة 4 (يوليو 2025) جعلتها SHALL للعامل الوحيد.
  7. **إلغاء certificate pinning الافتراضي.** توصية OWASP وAndroid الحالية ضده. البديل: فرض Certificate Transparency على Android.
  8. **حذف i18next-icu من الخطة.** i18next يعالج الجمع العربي الست صيغ أصلياً عبر Intl.PluralRules. إضافة إلزامية: polyfill `intl-pluralrules` على Hermes وإلا تنكسر العربية بصمت.
  9. **تعديل معيار قبول تبديل اللغة.** قلب الاتجاه RTL/LTR على native يتطلب إعادة تحميل واحدة (قيد منصة)، النصوص وحدها تتبدل فورياً.
  10. **Renovate إجباري وليس اختياراً.** Dependabot لا يدعم pnpm 11.
  11. أدوات مثبتة: pnpm 11.21، Turborepo 2.10، Vitest 4.1 (وjest-expo لمكونات RN)، Zod 4.4، Node 24 LTS، Drizzle 0.45.2 (لا ترقية لـ1.0 RC).
- **المراجع:** المصادر التفصيلية لكل قرار في `docs/TECH-STACK.md`.

## [2026-08-13] تأسيس المشروع وتوثيق الخطة

- **النوع:** توثيق + قرار تقني.
- **الوصف:** إنشاء التوثيق التأسيسي للمشروع: الخطة الكاملة، معايير الكتابة، وسجل التطوير هذا. تم التحقق من كل أدوات الستاك بتاريخ اليوم (2026-08-13) عبر بحث موسع في المصادر الرسمية، والنتائج موثقة في `docs/TECH-STACK.md`.
- **الملفات المتأثرة:**
  - `README.md`
  - `docs/PLAN.md`
  - `docs/TECH-STACK.md`
  - `docs/CODING-STANDARDS.md`
  - `docs/DEVLOG.md`
- **القرارات:**
  - اعتماد التوثيق الإلزامي: لا يُدمج أي تغيير بدون إدخال في هذا السجل.
  - التعليقات داخل الكود بالإنجليزية فقط.
  - نصوص الواجهة ورسائل الأخطاء لا تستخدم الشرطة الطويلة (—) إطلاقاً، الفصل بالفاصلة أو النقطة فقط.
  - الملفات تبقى صغيرة قدر الإمكان (انظر `docs/CODING-STANDARDS.md`).
- **المراجع:** انظر قسم المصادر في `docs/TECH-STACK.md`.
